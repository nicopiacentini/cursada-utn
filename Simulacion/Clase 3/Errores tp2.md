- Promedio de permanencia en el sistema de TODAS LAS COLAS = 
	SPS = SPS + (TPLL - T) *(NS1 + NS2)
- PPS por cola SPS_i = SPS_i + (TPS_i - T) * NS_i
- En ambos casos se hace luego de CADA EVENTO, es decir, despues de una salida y despues de una llegada